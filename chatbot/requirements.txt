# Chatbot Module Dependencies
# For standalone usage, install these packages
# For integrated usage, use root requirements.txt

# Core LLM and framework
langchain>=0.1.0
langchain-groq>=0.0.1
langchain-community>=0.0.1
langgraph>=0.0.20

# Data models
pydantic>=2.0.0

# Environment and utilities
python-dotenv>=1.0.0
typing-extensions>=4.5.0

# LangSmith tracing
langsmith>=0.0.40

# Memory and state management
# (uses in-memory storage, no additional deps needed)

